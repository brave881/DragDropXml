package com.example.dragdropxml.config.swipe.utils;

public enum SwipeState {
    IDLE, // Item is not moving
    SWIPING, // Item is moving because the user is swiping with the finger
    ANIMATING // Item is animating
}
